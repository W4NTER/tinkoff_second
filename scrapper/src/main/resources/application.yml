app:
  baseUrlGit: https://api.github.com/
  baseUrlStackOverFlow: https://api.stackexchange.com/2.3/questions/
  scheduler:
    enable: true
    interval: 10s
    force-check-delay: 10s

spring:
  application:
    name: scrapper

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml
