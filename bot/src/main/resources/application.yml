app:
  telegram-token: ${TELEGRAM_API_KEY}

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

client:
  port: 8090

server:
  base-url-sceapper: http://localhost:8089

# swagger-ui custom path
springdoc:
  swagger-ui:
    path: /swagger-ui

logging:
  config: classpath:log4j2-plain.xml